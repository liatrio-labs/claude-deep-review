Type: file
Path: skills/deep-review/references/phase1-preflight.md
Description: Phase 1 configuration gate — unconditional AskUserQuestion
Timestamp: 2026-03-31T00:00:00Z
Status: PASS

Verification:
  BF-04.1 — Table row for 0 unresolved items now requires AskUserQuestion: PASS
    Old: "No AskUserQuestion. Print confirmation: ..."
    New: "Still present AskUserQuestion with a single confirmation question (see 'Confirmation-only template' below). Never skip — Phase 2 checks that AskUserQuestion was called."

  BF-04.2 — Confirmation-only template added: PASS
    Section "Confirmation-only template (when REVIEW.md pre-configures both model_tier and default_delivery)" present
    Template includes AskUserQuestion with options:
      - "Yes — start review"
      - "No — change settings" (with re-run logic)

Evidence (dispatch table, row 0):
  | **0** | Still present AskUserQuestion with a single confirmation question (see "Confirmation-only template" below). Never skip — Phase 2 checks that AskUserQuestion was called. |

Evidence (confirmation-only template):
  AskUserQuestion(
    questions: [{
      question: "Ready to start. REVIEW.md configured: [mode] mode, delivering via [method]. Proceed?",
      header: "Review Configuration",
      ...
    }]
  )
