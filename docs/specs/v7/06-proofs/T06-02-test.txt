Type: test
Task: T06 — add missing tests
Command: python3 -m pytest tests/test_filter_findings.py::TestGroupByProximity::test_bucket_boundary_straddling tests/test_filter_findings.py::TestDedupCrossAgent::test_mixed_agent_group_same_agent_siblings_preserved -v --tb=short
Timestamp: 2026-04-07T10:30:00Z

FILTER FINDINGS REGRESSION TESTS (2 new tests added in d1216e5):
==================================================================

tests/test_filter_findings.py::TestGroupByProximity::test_bucket_boundary_straddling PASSED
tests/test_filter_findings.py::TestDedupCrossAgent::test_mixed_agent_group_same_agent_siblings_preserved PASSED

2 passed in 0.01s

Status: PASS

CONTEXT:
These tests were missing before commit d1216e5. They verify:

1. test_bucket_boundary_straddling:
   Verifies that a finding straddling a 15-line bucket boundary (e.g., line_start=10,
   line_end=20 spanning bucket 1-15 and 16-30) is placed in the correct bucket by
   its start line, not dropped.

2. test_mixed_agent_group_same_agent_siblings_preserved:
   Verifies that when cross-agent dedup processes a group with multiple agents,
   findings from the same agent are preserved as siblings (not deduplicated against
   each other), while cross-agent duplicates are deduplicated per the original logic.
   This was a regression in the V7-02 cross-agent dedup implementation.
